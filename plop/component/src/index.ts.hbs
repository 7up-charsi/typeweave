import * as {{camelCase name}} from "./{{name}}";

// export types
export type { {{capitalize name}}Props } from "./{{name}}";

// export component
export const {{capitalize name}} = {{camelCase name}}.default;
