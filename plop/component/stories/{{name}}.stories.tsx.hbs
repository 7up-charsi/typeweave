import React from 'react';

import { {{capitalize name}} } from '../src';

const meta = {
  title: 'Components/{{capitalize name}}',
};

export default meta;

const DefaultTemplate = (args) => <{{capitalize name}} {...args} />;

export const Default = {
  render: DefaultTemplate,
};
