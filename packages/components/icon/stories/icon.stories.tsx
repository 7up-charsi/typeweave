import React from "react";

import { Icon } from "../src";

const meta = {
  title: "Components/Icon",
};

export default meta;

const DefaultTemplate = (args) => <Icon {...args} />;

export const Default = {
  render: DefaultTemplate,
};
