import * as table from './table';

// export types
export type { TableProps, RootProps } from './table';

// export component
export const Root = table.Root;
export const Table = table.Table;
export const ColumnVisibility = table.ColumnVisibility;
