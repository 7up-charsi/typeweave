import { ThemeLayout } from './plugin-config-types';

export const defaultLayout: Required<ThemeLayout> = {
  borderRadius: '4px',
  boxShadow: {
    depthElevation:
      'rgba(0, 0, 0, 0.2) 0px 15px 30px, rgba(0, 0, 0, 0.1) 0px 5px 15px, rgba(0, 0, 0, 0.05) 0px 2px 8px',
  },
};
