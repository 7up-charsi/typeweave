import { ThemeLayout } from './types';

const borderRadius = '4px';

export const lightThemeLayout: ThemeLayout = {
  borderRadius,
};

export const darkThemeLayout: ThemeLayout = {
  borderRadius,
};
